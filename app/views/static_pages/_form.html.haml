=form_for(@newtimelog, url: :timelog) do |f|
	%div
		=f.label :description
		=f.text_area :description
	%div
		=f.label :time
		=f.time_field :time
	%div
		=f.label :kind
		=f.select :kind, Timelog.kinds
	%div
		=f.submit "submit"

